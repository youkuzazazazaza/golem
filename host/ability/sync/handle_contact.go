package sync

import (
	messageapi "github.com/sbgayhub/golem/host/api/message"
)

func handleContact(contacts []*messageapi.ModifyContact) {
	//for _, c := range contacts {
	//if _, err := contactability.Build(c); err != nil {
	//	slog.Error("构建联系人信息失败", "err", err)
	//} else {
	//	// todo))
	//}
	//}
}
